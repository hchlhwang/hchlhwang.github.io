# title: This is the Quiz Title
usemathjax: true
randomized: true
questions:

 - type: "multiple-choice"
   question: What is the $SO(3)$ for the rotation around the $z$-axis?  
   items:
    - choice: \begin{pmatrix}
            1 & 0 & 0\\
            0 & \cos\theta & -\sin\theta \\
            0 & \sin\theta & \cos\theta
            \end{pmatrix}
      correct: false
    - choice: \begin{pmatrix}
            1 & 0 & 0\\
            0 & \cos\theta & -\sin\theta \\
            0 & \sin\theta & \cos\theta
            \end{pmatrix}
      correct: false
    - choice: \begin{pmatrix}
            \cos\theta & -\sin\theta & 0 \\
            \sin\theta & \cos\theta & 0 \\
            0 & 0 & 1
            \end{pmatrix}
      correct: true
    - choice: \begin{pmatrix}
            \cos\theta & \sin\theta & 0 \\
            -\sin\theta & \cos\theta & 0 \\
            0 & 0 & 1
            \end{pmatrix}
      correct: false  #  followup: | 
  #     inside a paragraph, it will produce an inline expression, just like $$ E = mc^2 $$.

# To use display mode, again surround your expression with `$$` and place it as a separate paragraph. Here is an example:

# $$
# \sum_{k=1}^\infty |\langle x, e_k \rangle|^2 \leq \|x\|^2
# $$

# You can also use `\begin{equation}...\end{equation}` instead of `$$` for display mode math.
# MathJax will automatically number equations:

# \begin{equation}
# \label{eq:cauchy-schwarz}
# \left( \sum_{k=1}^n a_k b_k \right)^2 \leq \left( \sum_{k=1}^n a_k^2 \right) \left( \sum_{k=1}^n b_k^2 \right)
# \end{equation}

    
